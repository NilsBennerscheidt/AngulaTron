on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - run: node --version
      - run: npm --version
      - name: Chackout Code
        uses: actions/checkout@v2
      - name: Install dependencies 
        run: npm install
      - run: npm run electron 
