import { SortNeodByDatePipe } from './sort-neod-by-date.pipe';

describe('SortNeodByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortNeodByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
